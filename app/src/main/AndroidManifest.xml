<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.net.pwb">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="com.android.launcher.permission.READ_SETTINGS" />

    <application
        android:name="cn.at.pwb.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AtZxing">
        <activity
            android:name="cn.at.pwb.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AtZxing.NoActionBar" />
        <activity
            android:name="cn.at.pwb.ScannerActivity"
            android:configChanges="keyboardHidden|locale|orientation|screenLayout|screenSize|smallestScreenSize"
            android:theme="@style/Theme.Design.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>